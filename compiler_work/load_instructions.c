#include <stdlib.h>
#include <stdio.h>
#include "../shared/read_source_file.h"

int main() {
    char* source = read_source_file("/Users/abe/Documents/Programming/Emulator/programs/arithmatic_from_text.txt");
    // Need to roead the output from the compiler, which is a newline delimited set of instructions //
    // We should be able to 

    return 0;
}